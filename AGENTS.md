# 저장소 지침

이 저장소는 개인 Astro 사이트의 소스 코드와 공개 가능한 콘텐츠를 관리한다.

기존의 텍스트 중심 구조와 단순함을 유지하고, 필요한 범위에서만 수정한다.

## 저장소 관리

- 공개 저장소에는 사이트 소스와 공개 가능한 콘텐츠만 포함한다.
- 에이전트 작업 계획, 대화 기록, scratch note, 임시 보고서, `docs/superpowers` 등을 생성하거나 커밋하지 않는다.
- 임시 테스트 콘텐츠나 디버깅을 위해 생성한 파일을 최종 커밋에 포함하지 않는다.
- 변경에 필요한 파일만 정확히 stage한다.
- 서로 관계없는 변경을 하나의 commit이나 PR에 섞지 않는다.

## 구현 원칙

- Astro 사이트의 텍스트 중심이고 간결한 구조를 유지한다.
- 요구사항을 충족하는 가장 작은 변경을 우선한다.
- 작업에 반드시 필요하지 않다면 dependency를 추가하지 않는다.
- 명확한 필요가 없다면 새로운 abstraction, component, utility, infrastructure를 만들지 않는다.
- 새로운 구조를 만들기 전에 기존 프로젝트의 구조와 convention을 우선 따른다.
- 명시적인 요청이 없다면 기존 content validation과 Markdown processing 정책을 유지한다.
- 단순한 중복을 제거하기 위해 오히려 구조가 복잡해지는 리팩터링은 하지 않는다.
- 기존 코드가 충분히 단순하고 명확하다면 스타일 차이만을 이유로 다시 작성하지 않는다.

## 변경 범위

- 요청받은 작업에 직접 필요한 부분만 수정한다.
- 새로운 지침이나 convention을 기존 코드와 콘텐츠 전체에 임의로 소급 적용하지 않는다.
- 별도 요청 없이 주변 코드, 기존 콘텐츠, 파일 구조를 함께 정리하지 않는다.
- 현재 작업과 무관한 개선 사항을 발견하더라도 같은 변경에 섞지 않는다.

## 검증

- 변경 후 저장소에서 사용 중인 기존 검증 명령을 실행한다.
- 최소한 `npm run build`를 실행한다.
- 실행하지 않은 검증을 성공했다고 작성하지 않는다.
- 테스트를 위해 임시 파일을 만들었다면 검증 후 반드시 제거한다.
- 실패한 검증을 우회하거나 관련 설정을 약화해 통과시키지 않는다.

## Git

- 기존 저장소의 commit 및 PR convention을 따른다.
- 변경 목적에 맞게 `content:`, `feat:`, `fix:`, `chore:` 등의 prefix를 사용한다.
- commit message와 PR title은 실제 변경 내용을 간결하게 표현한다.
- 하나의 작업과 관계없는 변경을 같은 commit이나 PR에 포함하지 않는다.
- 최종 변경 파일을 확인한 뒤 의도한 파일만 stage한다.
